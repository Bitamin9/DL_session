모델이 너무 얕아보여서 모델 층을 키워주었다.
learning rate는 1e-4가 충분해보였다.
Adam 또한 훌륭한 optimizer라 바꾸지 않았다.

모델 크기를 키워서 epoch수는 10으로 충분할 것 같았다.
다만 생각보다 빨리 converge해서 epoch 수를 줄여도 될 것 같다.

더 좋은 모델 만들기 위해서는 CNN이라든가 도입해보면 좋겠다.